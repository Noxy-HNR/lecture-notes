# Python
venv/
__pycache__/
*.pyc

# Your actual lecture notes/recordings - personal data, not project source.
# Remove these lines if you want your notes tracked in this repo too.
notes/
state/
lessons/
# Frozen copy of the notes used by the meaning-search tests. Same reason as notes/ above -
# it's lecture content. Rebuild with: python tools/semantic_search_benchmark.py --freeze-corpus
tests/fixtures/semantic_search_corpus.json

# OS cruft
Thumbs.db
.DS_Store
